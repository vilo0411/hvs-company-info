---
description: "⚠️ Deprecated — dùng /write --no-serp thay thế"
allowed-tools: Read, Write, Bash
---

> **Lệnh này đã được thay thế bởi `/write`.**
>
> `/fast [keyword]` = `/write [keyword] --no-serp`

Chuyển sang `/write $ARGUMENTS --no-serp` và thực hiện theo `.claude/commands/write.md`.
